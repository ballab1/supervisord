#!/bin/bash

declare -ar env_www=(
    'WWW="${WWW:-/var/www}"'
    'WWW_UID="${WWW_UID:-$(www.UID)}"'
    'WWW_GID="${WWW_GID:-$(www.GID)}"'
    'WORKING_DIR="${WWW}"'
)

lib.updateRuntimeEnvironment "${env_www[*]}" 

lib.copyFileToRuntime 'LIB' 'www.bashlib'
